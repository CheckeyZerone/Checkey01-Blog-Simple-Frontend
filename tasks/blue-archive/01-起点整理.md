# T-D1 起点整理

**目标**：清理当前未提交的半成品改动，保留合理部分，提交一个干净的检查点。

## 当前未提交改动（2026-08-20 记录）

- 修改 `src/components/AppHeader.vue`：导航「标签」→「文章」（三段式布局已在之前提交完成）
- 修改 `src/components/PostCard.vue`：蓝稿风格（16px 圆角、日历图标、hover 上浮、featured 类、active 微缩）
- 修改 `src/router/index.ts`：`/post/:slug` → `/posts/:slug`
- 修改 `src/views/HomeViews.vue`：跳转改 `/posts/...`，删除旧注释代码
- 新建 `src/components/AlbumCard.vue`（空壳）、`src/components/CardList.vue`（空壳）
- 新建 `src/data/albums.ts`（Album 接口草稿）

## 步骤

1. 删除空壳 `AlbumCard.vue` / `CardList.vue`（T-D8 按最终设计重写，不提前留半成品）
2. 保留 `albums.ts` 作为相册数据模型起点，确认它暂未被引用（避免 TS 未使用报错）
3. 复核 PostCard 用到的新 CSS 变量（`--text-info`、`--main-blue`、`--light-blue`、`--border`）在 `src/assets/main.css` 已定义
4. 运行 `npm run dev` 验证：导航含「文章」、PostCard 新样式正常、`/posts/:slug` 详情可进
5. 跑完整审查清单后提交（只提交本任务涉及文件，不打包无关改动）

**完成标志**：工作区干净；导航、卡片、路由均正常；空壳文件已删除。

**审查重点**：未定义 CSS 变量、空壳文件残留、路由改动是否影响详情页/标签页。

**本任务概念**：提交粒度（一个任务一个 commit）、工作区清理。
