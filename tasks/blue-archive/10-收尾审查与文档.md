# T-D10 收尾审查与文档

**目标**：全站动效一致性、三档响应式终检、全量代码审查，并把 T-D 系列状态更新进 LEARNING_PLAN.md，收工提交。

## 步骤

1. 动效统一：按钮 / 卡片 / 缩略按钮同一套 hover 上浮 + active 缩放参数；`prefers-reduced-motion: reduce` 时全部关闭
2. 响应式三档目测：1024px（两列）→ 736px（两列）→ 320px（单列），页面：首页 / 文章 / 相册 / 内容页 / 友链
3. 全量审查：按 `tasks/README.md` 通用清单 8 项走一遍（死链、未使用文件、重复样式、空壳、数据入口）
4. 核对四条原则是否被遵守（数据解耦 / 审查 / 无旧页面残留 / 复用）
5. 更新 `LEARNING_PLAN.md`：当前状态、任务索引勾选、开发日志追加
6. 提交（是否推送按用户要求）

**完成标志**：三档断点正常；lint / type-check / build 通过；文档与代码一致；LEARNING_PLAN.md 已更新。

**本任务概念**：上线前回归测试、文档同步。
